# For more information see:
# https://thegraybook.vvvv.org/reference/extending/publishing.html

name: push_nuget

# on push on main
on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Git Checkout
      uses: actions/checkout@main
#      with:
#        submodules: 'true'
#        lfs: 'true'

    - name: Setup .NET 10 SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Setup MSBuild.exe
      uses: microsoft/setup-msbuild@v3

    - name: Setup Nuget.exe
      uses: nuget/setup-nuget@v4.0

    - name: Publish VL Nuget
      uses: vvvv/PublishVLNuget@1.0.47
      with:
        csproj: src\VL.Stride.glTF.csproj
        nuspec: deployment\VL.Stride.glTF.nuspec

        # Fill the Icon file
        icon-src: https://raw.githubusercontent.com/vvvv/PublicContent/master/nugeticon.png
        icon-dst: ./deployment/nugeticon.png
        
        # Provide the nuget key. See: https://thegraybook.vvvv.org/reference/extending/publishing.html#getting-a-nugetorg-api-key
        nuget-key: ${{ secrets.NUGET_KEY }}
